# Code fait par Rayan Belhamra
#14 Septembre 20222
#TP1
#La fonction du code est de déterminer le nombre de mots dans une phrase
phrase = input("désire tu écrire une phrase et on va compter le nombre de mot:")
#grâce a la fonction de input, ca prmet a l'utiisateur du code d'écrir dans la console
print(phrase)
#crée une foncttionne qui va s'appeler main
def main():
    #on crée une variable qui permet de split le sting en une liste
    contage = phrase.split()
    #on cré variable len pour conter le nombre de caractere dans un stirng
    nombredemot = len(contage)
    #on utilise print pour ecrire la phrase afin que le joueur sache le nombre de mot
    print("dans cette phrase, le nombre de mot est :", nombredemot)
main()
